export enum ModelTypeName {
  USER = "user",
  MEMORY = "memory",
  ALBUM = "album",
  PROFILE = "profile",
}

export enum ImageTypeName {
  IMAGES = "images",
  WALL = "wallImage",
  AVATAR = "avatar"
}
